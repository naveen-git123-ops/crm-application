[Unit]
Description=CRM FastAPI (uvicorn)
After=network.target

[Service]
User=ec2-user
WorkingDirectory=/home/ec2-user/crm-application/backend
ExecStart=/usr/bin/python3 -m uvicorn server:app --host 0.0.0.0 --port 8000
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
